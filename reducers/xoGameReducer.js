let initialState = {
    boardArr: [8],
    next: "x"
};
initialState.boardArr[0] = "x";


const xoGameReducer = (state = initialState, action) => {
    switch (action){
        default:
            return state
    }
};


export default xoGameReducer